import 'package:flutter/material.dart';

const blueStyle1 = Color.fromRGBO(240,255,255,1);
const blueStyle2 = Color.fromRGBO(0,0,139,1);

const whiteStyle1 = Color.fromRGBO(230,230,230,1); // 텍스트 필드 배경 색상

const blueStyle3 = Color.fromRGBO(100,149,237,1);
const blueStyle4 = Color.fromRGBO(135,206,250,1);
const blackStyle1 = Color.fromRGBO(10,10,32,0.9); // 버튼 위의 글씨용 (검정)
const whiteStyle2 = Color.fromRGBO(244,244,244,1); // 버튼 위의 글씨용 (하양)

const greyStyle1 = Color.fromRGBO(211,211,211,0.8);

const blueStyle5 = Color.fromRGBO(4,4,180,1); // 앱 바 색상 + 버튼 색상
const blueStyle6 = Color.fromRGBO(238,233,250,1);// 백그라운드 배경 (메인)

const whiteStyle3 = Color.fromRGBO(190,198,205,1);// 카드 사이 가르는 거
