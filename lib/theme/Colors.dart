import 'package:flutter/material.dart';

const blueStyle1 = Color.fromRGBO(240,255,255,1); // 다이얼로그 배경
const blueStyle2 = Color.fromRGBO(0,0,139,1); // 다이얼로그 테두리


const whiteStyle1 = Color.fromRGBO(230,230,230,1); // 기본 배경

const blueStyle3 = Color.fromRGBO(100,149,237,1); // 버튼용 (메인)
const blueStyle4 = Color.fromRGBO(135,206,250,1); // 버튼용 (서브)
const blackStyle1 = Color.fromRGBO(10,10,32,0.9); // 버튼 위의 글씨용
const greyStyle1 = Color.fromRGBO(211,211,211,0.8);// 텍스트 필드 배경 색상